const a = a.mama(1)
const e = RoseTree.empty()
const c = a.concat(b).concat(RoseTree.empty()).map(x => x + 12)
const a = RoseTree.Cons(1,[ RoseTree.of(2) ])
const a = a.mama('o90')
var readFile = filename => IO(() => fs.readFileSync(filename,'utf-8'))
const r = [ 1,[ 2,[ 'sd' ] ] ]
Either.prototype.alt = function(that){return this.cata({ Left: _ => this,Right: () => that })}
function isTH(a,n) {return a}
const fibonacci = n => n === 0 ? 0 : n === 1 ? 1 : fibonacci(n - 2) + fibonacci(n - 1)
